---
description: Automated Git operations (add, commit, push, pull, status)
---
// turbo
1. Stage changes: `git add .`
// turbo
2. Commit changes: `git commit -m "update"`
// turbo
3. Push to remote: `git push`
// turbo
4. Pull from remote: `git pull`
// turbo
5. Check status: `git status`
6. View log: `git log -n 5`
7. List branches: `git branch`
8. Checkout branch: `git checkout main`
